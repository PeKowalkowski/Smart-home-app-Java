package com.smartHomeApp.SmartHomeApp.user.application.services.UserService;

public class UserService {
 /* - getProfile(Long userId)
  - updateProfile(Long userId, UpdateProfileRequest)
  - changePassword(Long userId, oldPassword, newPassword)
  - lockUser(Long userId, reason)
  - unlockUser(Long userId)
  - revokeSessions(Long userId)*/
}
